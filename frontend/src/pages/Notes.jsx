import { PDFUploadCard, QuizCard, FlashcardCard } from "../components";

function Notes() {
    return (
        <div>
            <PDFUploadCard />
            <QuizCard />
            <FlashcardCard />
        </div>
    );
}

export default Notes;
